{
  "name": "planfixfix",
  "version": "0.6.2",
  "description": "Userscript for Chrome and Firefox for fast analytics insert. Only for tagilcity.planfix.ru",
  "scripts": {
    "version": "npm run build && npm run changelog && git add CHANGELOG.md planfixfix.user.js",
    "postversion": "git push && npm run release",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s -r 0",
    "release": "conventional-github-releaser -p angular",
    "build": "bash scripts/build.sh"
  },
  "author": "Stanislav Popov",
  "license": "ISC"
}
